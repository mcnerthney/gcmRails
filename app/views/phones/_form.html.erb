<%= form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deviceid %><br />
    <%= f.text_area :deviceid %>
  </div>
  <div class="field">
    <%= f.label :gcmid %><br />
    <%= f.text_area :gcmid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
